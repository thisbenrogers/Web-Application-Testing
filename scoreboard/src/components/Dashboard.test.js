import { clickStrike, clickBall, clickFoul, strikes, setStrikes, balls, setBalls } from '../App';

describe('Dashboard.js', () => {
  describe('clickStrike()', () => {
    it('should increment strikes to 3 then reset strikes and balls', () => {
      expect(clickStrike()).
    })
  })
  describe('clickBall()', () => {

  })
  describe('clickFoul()', () => {

  })
})